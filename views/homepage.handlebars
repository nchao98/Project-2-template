<!--homepage -->
<!-- main start  -->
            <main>
                <div class="container-fluid px-4">
                  <h1 class="mt-4" style="text-align: center;">Welcome!</h1>               
                  {{!-- <h2 class="mt-4" style="text-align: center;">{{ ideas.[0].idea_name }}</h2> --}}
                  
            <!-- carousel start  -->
            
            <div id="carouselExampleCaptions" class="carousel slide" data-bs-ride="carousel">
              <div class="carousel-indicators">
                
                <button type="button" data-bs-target="#carouselExampleCaptions" data-bs-slide-to="0" class="active" aria-current="true" aria-label="Slide 1"></button>
                <button type="button" data-bs-target="#carouselExampleCaptions" data-bs-slide-to="1" aria-label="Slide 2"></button>
                <button type="button" data-bs-target="#carouselExampleCaptions" data-bs-slide-to="2" aria-label="Slide 3"></button>
              </div>
              
              <div class="carousel-inner">
                <div class="carousel-item active" >
                    <iframe
                        src="{{ ideas.[0].link_name }}" width="100%" height="100%">
                    </iframe>
                </div>
                <div class="carousel-item">
                    <iframe 
                        src="{{ ideas.[1].link_name }}" width="100%" height="100%">
                    </iframe>
                </div>
                <div class="carousel-item">                 
                    <iframe 
                        src="{{ ideas.[2].link_name }}" width="100%" height="100%">
                    </iframe>                 
                </div>
              </div>

              <button class="carousel-control-prev" type="button" data-bs-target="#carouselExampleCaptions" data-bs-slide="prev">
                <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                <span class="visually-hidden">Previous</span>
              </button>
              
              <button class="carousel-control-next" type="button" data-bs-target="#carouselExampleCaptions" data-bs-slide="next">
                <span class="carousel-control-next-icon" aria-hidden="true"></span>
                <span class="visually-hidden">Next</span>
              </button>
            </div>
            <!-- carousel end   -->

            <!-- description start -->
            <div class="card mb-4">
              <div class="card-header">
                  <i class="fas fa-book-open me-1"></i>
                  Welcome to ShareSp@ce!
              </div>
              <div class="card-body" style="text-align: center;"> 
                  The place to share all of your favorite ideas, pictures, videos, and GIFs about 10 of the most popular subjects on the web!
              </div>
            </div>
            <!-- description end -->
            
    </main>
    